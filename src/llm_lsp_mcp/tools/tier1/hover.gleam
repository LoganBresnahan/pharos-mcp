//// MCP tool: `hover`.
////
//// Returns type signature and documentation for the symbol at a
//// given position (URI + line + character). Maps to LSP
//// `textDocument/hover`. Renders LSP `MarkupContent` (markdown or
//// plaintext) into a single text content block.
////
//// Stub — lands in Milestone 4.
