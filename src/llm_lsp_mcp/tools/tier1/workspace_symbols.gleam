//// MCP tool: `workspace_symbols`.
////
//// Project-wide symbol search by name (substring match). Maps to LSP
//// `workspace/symbol`. Results capped at a configurable limit to
//// avoid bloating the LLM context.
////
//// Stub — lands in Milestone 4.

/// Placeholder so Gleam does not flag this as an empty module.
/// Removed in the milestone that implements this module.
pub const placeholder: Nil = Nil
