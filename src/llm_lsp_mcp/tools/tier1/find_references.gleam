//// MCP tool: `find_references`.
////
//// Returns all usages of a symbol across the workspace. Maps to LSP
//// `textDocument/references` with `includeDeclaration: true` by
//// default (overridable). Results grouped by file in the content
//// block for readability.
////
//// Stub — lands in Milestone 4.

/// Placeholder so Gleam does not flag this as an empty module.
/// Removed in the milestone that implements this module.
pub const placeholder: Nil = Nil
