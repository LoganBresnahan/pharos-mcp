//// Library entry / facade for `llm_lsp_mcp`.
////
//// Re-exports types and functions intended for external consumers
//// (tests, embedders, future library users). The CLI entrypoint is
//// `llm_lsp_mcp/application` — invoked by the OTP application boot
//// sequence wired in `mix.exs`.
////
//// Stub — implementation lands incrementally per milestones in
//// `doc/init.md`.
